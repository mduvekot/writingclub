# Function to randomize sentences in a markdown file
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
}
randomize_sentences("writingclube/2025-11-13.md")
randomize_sentences("writingclub/2025-11-13.md")
library(magrittr)
# Function to randomize sentences in a markdown file
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
}
randomize_sentences("writingclub/2025-11-13.md")
# Load required library
library(dplyr)
library(magrittr)
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
}
randomize_sentences("writingclub/2025-11-13.md")
# Print randomized text
cat(paste(df_random$sentence, collapse = "\n"), "\n")
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
cat(paste(df_random$sentence, collapse = "\n"), "\n")
}
randomize_sentences("writingclub/2025-11-13.md")
randomize_sentences("writingclub/2025-11-13.md")
randomize_sentences("writingclub/2025-11-13.md")
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
}
randomize_sentences("writingclub/2025-11-13.md")
print(randomize_sentences("writingclub/2025-11-13.md"))
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
df <- df %>% filter(!grepl("^%%.*%%$", sentence))
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
cat(paste(df_random$sentence, collapse = " "), "\n")
}
print(randomize_sentences("writingclub/2025-11-13.md"))
# Print randomized text
paste(df_random$sentence, collapse = " ")
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
df <- df %>% filter(!grepl("^%%.*%%$", sentence))
# Randomize row order
df_random <- df %>% slice_sample(n = nrow(df))
# Print randomized text
paste(df_random$sentence, collapse = " ")
}
print(randomize_sentences("writingclub/2025-11-13.md"))
cat(randomize_sentences("writingclub/2025-11-13.md"))
# increase the maximum number of items printed
options(max.print = 999999)
cat(randomize_sentences("writingclub/2025-11-13.md"))
print(randomize_sentences("writingclub/2025-11-13.md"))
print(randomize_sentences("writingclub/2025-11-13.md"), max = Inf)
# increase the maximum number of items printed
options(max.print = 999999)
print(randomize_sentences("writingclub/2025-11-13.md"), max = Inf)
print(randomize_sentences("writingclub/2025-11-13.md"))
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
df <- df %>% filter(!grepl("^%%.*%%$", sentence))
# Randomize row order
df %>% slice_sample(n = nrow(df))
}
randomize_sentences("writingclub/2025-11-13.md")
randomize_sentences("writingclub/2025-11-13.md") |> pull(sentences)
randomize_sentences("writingclub/2025-11-13.md") -> foo
randomize_sentences("writingclub/2025-11-13.md") |> pull(sentence)
randomize_sentences("writingclub/2025-11-13.md") |> pull(sentence) |>  paste()
randomize_sentences("writingclub/2025-11-13.md") |> pull(sentence) |>  paste(collapse = " ")
randomize_sentences("writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
cat()
filepath = "writingclub/2025-11-13.md"
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
text
text <- readLines(file_path, warn = FALSE)
file_path = "writingclub/2025-11-13.md"
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
df <- df %>% filter(!grepl("^%%.*%%$", sentence))
View(df)
df %>% filter(!grepl("^%%.*%%$", sentence))
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
df <- df %>% filter(!grepl("^%%.*%%$", sentence))
View(df)
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
df <- df %>% filter(!grepl("^%%.*%%$", sentence))
# Randomize row order
df %>% slice_sample(n = nrow(df))
View(df)
text <- gsub("%%.*?%%", "", text)
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
View(df)
randomize_sentences <- function(file_path) {
# Read the entire markdown file as text
text <- readLines(file_path, warn = FALSE)
# Collapse into one string (so line breaks don't interfere)
text <- paste(text, collapse = " ")
text <- gsub("%%.*?%%", "", text)
# Split into sentences using regex (., ?, ! followed by space)
sentences <- unlist(strsplit(text, "(?<=[.!?])\\s+", perl = TRUE))
# Create dataframe: one row per sentence
df <- data.frame(sentence = sentences, stringsAsFactors = FALSE)
# Filter out sentences that are comments (wrapped in %% ... %%)
# df <- df %>% filter(!grepl("^%%.*%%$", sentence))
# Randomize row order
df %>% slice_sample(n = nrow(df))
}
# increase the maximum number of items printed
options(max.print = 999999)
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
cat()
getOption("max.print")
# increase the maximum number of items printed
options(max.print = 999999)
getOption("max.print")
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
print.default()
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
print.default(max = 1000)
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
print.default(max = 1000)
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
print.default(max = 1000)
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
print.default(max = 1000)
randomize_sentences(file_path = "writingclub/2025-11-13.md") |>
pull(sentence) |>
paste(collapse = " ") |>
print.default(max = 1000)
